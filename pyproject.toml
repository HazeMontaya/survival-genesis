[build-system]
requires=["setuptools>=68"]
build-backend="setuptools.build_meta"
[project]
name="survival-genesis"
version="0.2.0"
description="Zero-capital-first autonomous runtime"
requires-python=">=3.10"
dependencies=[]
[project.scripts]
survival-genesis="genesis.cli:main"
[tool.setuptools.packages.find]
include=["genesis*"]
[tool.pytest.ini_options]
testpaths=["tests"]
